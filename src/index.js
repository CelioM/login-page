import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import PageRots from './routes/routes'

ReactDOM.render(
  <React.StrictMode>
    
      <PageRots/>
    
  </React.StrictMode>,
  document.getElementById('root')
);

